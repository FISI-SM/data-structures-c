# Tabla Hash (separate chaining) en C++

`HashTable<K,V>` genérica usando listas por cubeta. Incluye `insert`, `erase`, `at`, `contains`, `rehash`, `imprimir`.

## Compilar/Ejecutar
```bash
make
./bin/app_hash_table
```
