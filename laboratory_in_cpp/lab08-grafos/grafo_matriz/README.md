# Grafo con Matriz de Adyacencia (arrays) en C++

Implementa `GrafoMatriz` con `int**` y 0/1. Incluye `agregarArista`, `eliminarArista`, `existeArista`, `imprimir`, `bfs`, `dfs`.

## Compilar y ejecutar
```bash
make
./bin/app_grafo_matriz
```
