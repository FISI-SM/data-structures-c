# Grafo con Lista de Adyacencia (lista enlazada) en C++

Implementa `GrafoLista` ponderado con un arreglo de punteros a listas (`NodoAdj`). Operaciones: `agregarArista`, `eliminarArista`, `existeArista`, `imprimir`, `dijkstra`.

## Compilar y ejecutar
```bash
make
./bin/app_grafo_lista
```